import React from 'react';
import {post} from "./utils/Request";

const MessageManage = () => {
    const init = () => {

    }

    React.useEffect(()=>{
        init();
    },[]);

    return (
        <div>

        </div>
    );
};

export default MessageManage;
