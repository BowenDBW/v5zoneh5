import React from 'react';

const ImageManage = () => {

    const init = () => {

    }

    React.useEffect(()=>{
        init();
    },[]);

    return (
        <div>

        </div>
    );
};

export default ImageManage;
